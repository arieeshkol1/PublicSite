name: Deploy UI to S3 (DISABLED)

on:
  # all triggers disabled
  push: null
  pull_request: null
  workflow_dispatch: null
  schedule: null

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: This workflow is disabled
        run: |
          echo "⚠️ The 'Deploy UI to S3' workflow has been disabled and will not run."
