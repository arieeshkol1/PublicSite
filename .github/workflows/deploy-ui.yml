name: Deploy UI to S3 (DISABLED)

on:
  # Valid triggers that will never match
  push:
    branches-ignore:
      - '**'
  pull_request:
    branches-ignore:
      - '**'
  # NOTE: no workflow_dispatch here, so it can't be started manually

jobs:
  disabled:
    if: ${{ false }}  # second guard; even if triggered, job is skipped
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled."
